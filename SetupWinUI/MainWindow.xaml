<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SetupWinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SetupWinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid
    x:Name="AppTitleBar"
    Grid.Row="0"
    Height="30"
    Margin="0,0,0,0"
    Padding="0,0,0,0"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Top">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <!-- Для иконки -->
                <ColumnDefinition Width="*"/>
                <!-- Для текста -->
            </Grid.ColumnDefinitions>

            <!-- Иконка слева -->
            <ImageIcon
        x:Name="TitIcon"
        Grid.Column="0"
        Width="20"
        Height="20"
        Margin="8,0,8,0"
        VerticalAlignment="Center"
        Source="ms-appx:///Assets/StoreLogo.scale-400.png" />

            <!-- Надпись справа от иконки -->
            <TextBlock
        Grid.Column="1"
        Text="Установщик VK M"
        VerticalAlignment="Center"
        Margin="0,0,8,0"/>

        </Grid>




        <StackPanel Grid.Row="1" Margin="24" Spacing="16">
            <TextBlock x:Name="label1" Text="VK M — музыкальный клиент. Здесь вы можете скачать последнюю версию приложения." TextWrapping="Wrap" FontSize="16"/>
        
                <TextBlock x:Name="label2" Text="Актуальная версия:" FontWeight="Bold" FontSize="14"/>
                <TextBlock x:Name="label10" Text="0.0.0.0" FontSize="14"/>
   
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock x:Name="label3" Text="Название выпуска:" FontWeight="Bold" FontSize="14"/>
                <TextBlock x:Name="label9" Text="" FontSize="14"/>
            </StackPanel>
            <TextBlock x:Name="label4" Text="Что нового:" FontWeight="Bold" FontSize="14"/>
            <TextBox x:Name="whatsNews" IsReadOnly="True" Height="80" TextWrapping="Wrap"/>
                <RadioButton x:Name="MSIXRadio" Content="MSIX пакет (рекомендуется)" IsEnabled="False" Checked="Radio_CheckedChanged"/>
                <RadioButton x:Name="EXERadio" Content="EXE (Если не установлен MS Store или не ставится MSIX)" IsEnabled="False" Checked="Radio_CheckedChanged"/>
     
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock x:Name="label5" Text="Размер файла:" FontWeight="Bold" FontSize="14"/>
                <TextBlock x:Name="label7" Text="0 МБ" FontSize="14"/>
            </StackPanel>
            <ProgressBar x:Name="progressBar1" Height="20"/>
            <TextBlock x:Name="label6" Text="0 Мб" FontSize="12"/>
            <Button x:Name="button1" Content="Установить" Height="50" Click="button1_Click" IsEnabled="False"/>
            <CheckBox x:Name="checkBox1" Content="Принудительно установить пакеты"/>
            <TextBlock x:Name="label11" Text="(Активируйте, если у Вас отключены некоторые компоненты и службы Windows, или просто не устанавливается по причине невозможности установить пакеты зависимостей.)" TextWrapping="Wrap" FontSize="12"/>
            <Button x:Name="button2" Content="Тех поддержка" Height="30" Click="button2_Click"/>
            <TextBox x:Name="logTextBox" Height="100" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True"/>
        </StackPanel>
    </Grid>
</Window>

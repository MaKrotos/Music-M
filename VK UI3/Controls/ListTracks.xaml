<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="VK_UI3.Controls.ListTracks"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VK_UI3.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:custom="using:VK_UI3.Controls"
    Margin="10"
    mc:Ignorable="d">
    <UserControl.Shadow>
        <ThemeShadow />
    </UserControl.Shadow>
    <Grid>
        <ScrollView>
            <GridView  SelectionMode="None" ItemsSource="{x:Bind _tracks, Mode=OneWay}">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Vertical" MaximumRowsOrColumns="2"/>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>

                <GridView.ItemTemplate>
                    <DataTemplate>
                        <custom:TrackControl
                        
                         FocusVisualPrimaryBrush="Transparent"
                         FocusVisualSecondaryThickness="0"
                         ></custom:TrackControl>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </ScrollView>
    </Grid>
</UserControl>

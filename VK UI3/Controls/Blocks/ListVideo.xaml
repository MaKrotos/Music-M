<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="VK_UI3.Controls.Blocks.ListVideo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:custom="using:VK_UI3.Controls"
    xmlns:customV="using:VK_UI3.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:VK_UI3.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="userC"
    Margin="0,0,0,0"
    mc:Ignorable="d">
    <UserControl.Resources>
        <DataTemplate x:Key="default">
            <local:PlaylistControl />
        </DataTemplate>
        <DataTemplate x:Key="compact">
            <local:VibeControl />
        </DataTemplate>
    </UserControl.Resources>
    <UserControl.Shadow>
        <ThemeShadow />
    </UserControl.Shadow>




    <Grid
        x:Name="gridCh"
        Height="Auto"
        CornerRadius="8">


        <customV:UniversalControl
            x:Name="myControl"
            ItemsSource="{x:Bind video}"
            itsAll="{x:Bind itsAll}">

            <customV:UniversalControl.ItemTemplate>
                <DataTemplate>
                    <custom:VideoController FocusVisualPrimaryBrush="Transparent" FocusVisualSecondaryThickness="0" />
                </DataTemplate>
            </customV:UniversalControl.ItemTemplate>
        </customV:UniversalControl>
    </Grid>
</UserControl>
